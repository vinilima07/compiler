program teste1 is
declare
    a, b: int;
    result : int;
    a,x : float
init
    a = 12;
    x = 12;
    in << a;
    in << b;
    in << c;
    result = (a*b + 1) / (c+2);
    out >> "Resultado: ";
    out >> result
end

